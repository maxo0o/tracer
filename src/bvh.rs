use std::ops::Bound;

use crate::aabb::AxisAlignedBoundingBox;

pub struct BoundingVolumeHierarchy {
    pub bounding_box: AxisAlignedBoundingBox,
}

impl BoundingVolumeHierarchy {
    pub fn new() -> BoundingVolumeHierarchy {
        
    }
}